Directory: exercises/core-02

공식 `nginx` image를 pull하고 실행한 뒤, port 80을 노출하여 로컬에서 접속이 가능한지 확인하세요.

1. `nginx` container를 실행합니다.
2. host port `8080`을 container port `80`에 매핑합니다.
3. `curl http://localhost:8080`으로 작동 여부를 확인합니다.

